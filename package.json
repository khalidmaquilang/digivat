{
  "$schema": "https://json.schemastore.org/package.json",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "vite build",
    "dev": "vite",
    "prepare": "husky",
    "pre-commit": "composer rector && php artisan ide-helper:generate && php artisan ide-helper:models -RW && php vendor/bin/pint && node_modules\\.bin\\prettier --write resources\\**\\*.blade.php && git add ."
  },
  "devDependencies": {
    "@shufo/prettier-plugin-blade": "^1.16.1",
    "@tailwindcss/vite": "^4.0.0",
    "axios": "^1.11.0",
    "concurrently": "^9.0.1",
    "husky": "^9.1.7",
    "laravel-vite-plugin": "^2.0.0",
    "prettier": "^3.6.2",
    "tailwindcss": "^4.0.0",
    "vite": "^7.0.4"
  }
}
